# 4C dossiers (per-bug fix records) — local working state, not committed
.4c/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Editor directories and project files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Logs
*.log

# Plugin install/build artifacts (if added later)
node_modules/
dist/
*.zip

# Transient lane state (worktrees, pre-repair snapshots, quarantined paths) -
# never committed
.worktrees/
.backup/
.quarantine/
